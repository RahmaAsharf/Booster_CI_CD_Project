name: build and push Docker Image
on:
  push:
    branches:
      - "master"
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build Docker Image
        run: docker build . --file dockerfile -t rahmaashraf20/django-app-jenkinslab

      - name: Login to Docker Hub
        run: docker login -u rahmaashraf20 -p RahmaA$raf2002
      
      - name: Push Docker image
        run: docker push rahmaashraf20/django-app-jenkinslab
